table! {
    users (id) {
        id -> Varchar,
        first_name -> Text,
        last_name -> Text,
        email -> Varchar,
        company -> Text,
        password -> Text,
        created_at -> Timestamp,
    }
}
